def prime(n):
    for i in range(2,n):
        if n%i==0:
            print('prime')
        else:
            print('composite')
        t=int(input())
        for i in range(t):
            n=int(input())
            prime(n)
        
